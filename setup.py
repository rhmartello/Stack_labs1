from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Projeto do stack labs com foco em predição de bitcoins',
    author='Hive',
    license='MIT',
)
